<!-- Hidden processing chapter: saving data -->

```{r, include=FALSE}
# Save temperature data
write.csv(Temperatures,
          file = "data_Güttingen/Temperatures_ch12.csv",
          row.names = FALSE)

# Create example list
test_list <- list(
  Number = 1,
  String = "Thanks for using chillR!",
  DataFrame = data.frame(
    a = c(1,2,3),
    b = c(3,2,1),
    c = c(5,4,3)
  )
)

# Save list using chillR
save_temperature_scenarios(test_list,
                           path = "data",
                           prefix = "test_list")
```